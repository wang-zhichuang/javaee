<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.BankDao">	

	
	<update id="inMoney">
		update t_count set count=count+#{param1} where Id=#{param2}
	</update>
	
	<update id="outMoney">
		update t_count set count=count-#{param1} where Id=#{param2}
	</update>
</mapper> 